name: Github Actions 실행하기

on:
  push:
    branches:
      - main
jobs:
  Manual-deployment-Job:
    runs-on: ubuntu-latest

    steps:
      - name: applicatio.yml 생성
        run: | 
          pwd
          ls
          cd ./src/main/
          ls
          echo ${{secrets.APPLICATION_YML}} > src/main/resources/application.yml